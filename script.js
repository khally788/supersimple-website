// Example: Updating the cart count
const cartCount = document.getElementById('cart-count');
const initialCartCount = 0; // Replace with actual initial cart count

cartCount.textContent = initialCartCount; // Set initial cart count
